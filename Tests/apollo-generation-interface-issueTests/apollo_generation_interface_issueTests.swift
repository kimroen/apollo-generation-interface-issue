import XCTest
@testable import apollo_generation_interface_issue

final class apollo_generation_interface_issueTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(apollo_generation_interface_issue().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
